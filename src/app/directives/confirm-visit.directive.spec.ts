import { ConfirmVisitDirective } from './confirm-visit.directive';

describe('ConfirmVisitDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmVisitDirective();
    expect(directive).toBeTruthy();
  });
});
